{
    "scripts": {
        "dev": "nodemon src/index.ts",
        "build": "rimraf build && tsc",
        "start": "npm run build && node build/index.js"
    },
    "devDependencies": {
        "@types/node": "^18.14.2",
        "nodemon": "^2.0.20",
        "rimraf": "^4.1.2",
        "ts-node": "^10.9.1",
        "typescript": "^4.9.5"
    }
}
